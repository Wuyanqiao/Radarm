# Radarm 环境变量配置示例
# 复制此文件为 .env 并填入实际的配置值

# ==================== API 配置 ====================
# DeepSeek API 配置
# 默认基础URL（可选，如果使用自定义API代理）
DEEPSEEK_BASE_URL=https://api.deepseek.com/chat/completions

# DeepSeek A/B/C 三个槽位的独立配置（可选）
# 如果未设置，将使用 DEEPSEEK_BASE_URL 和默认模型名
# DEEPSEEK_A_URL=https://api.deepseek.com/chat/completions
# DEEPSEEK_A_MODEL=deepseek-reasoner
# DEEPSEEK_B_URL=https://api.deepseek.com/chat/completions
# DEEPSEEK_B_MODEL=deepseek-reasoner
# DEEPSEEK_C_URL=https://api.deepseek.com/chat/completions
# DEEPSEEK_C_MODEL=deepseek-reasoner

# 智谱AI API 配置
ZHIPU_BASE_URL=https://open.bigmodel.cn/api/paas/v4/chat/completions
ZHIPU_VISION_MODEL=glm-4v

# 通义千问 API 配置
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
QWEN_VISION_MODEL=qwen-vl-plus

# ==================== 视觉模型配置 ====================
# 视觉模型参数（用于图片理解）
# 最大图片数量
VISION_MAX_IMAGES=3
# 图片最大边长（像素）
VISION_MAX_EDGE=1024
# 图片最大字节数
VISION_MAX_BYTES=2000000
# 视觉模型请求超时时间（秒）
VISION_TIMEOUT=120

# ==================== 服务器配置 ====================
# 后端服务端口（默认8000）
# BACKEND_PORT=8000
# 前端服务端口（默认3000，仅开发模式）
# FRONTEND_PORT=3000

# ==================== 数据存储配置 ====================
# 会话数据存储路径（相对于项目根目录）
# DATA_DIR=radarm_data
# 输出文件存储路径（图表、报告等）
# OUT_DIR=out

# ==================== 数据库配置（可选）====================
# 如果使用数据库连接功能，可以配置默认连接信息
# MYSQL_HOST=localhost
# MYSQL_PORT=3306
# MYSQL_USER=root
# MYSQL_PASSWORD=your_password
# MYSQL_DATABASE=your_database

# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_password
# POSTGRES_DATABASE=your_database

# ==================== 其他配置 ====================
# 是否启用调试模式
# DEBUG=False

# CORS 允许的来源（生产环境建议设置具体域名）
# CORS_ORIGINS=http://localhost:3000,http://localhost:8000

